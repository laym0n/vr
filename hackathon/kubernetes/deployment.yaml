apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    app: api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: cache-service
          image: verhovnick/cache-service:v3
          ports:
            - containerPort: 8000
          resources:
            requests:
              memory: "32Mi"
              cpu: "25m"
            limits:
              memory: "64Mi"
              cpu: "50m"
        - name: generate-schema-service
          image: verhovnick/generate-schema-service:v3
          ports:
            - containerPort: 8001
          resources:
            requests:
              memory: "32Mi"
              cpu: "25m"
            limits:
              memory: "64Mi"
              cpu: "50m"
        - name: request-validator-service
          image: verhovnick/request-validator-service:v4
          ports:
            - containerPort: 8002
          resources:
            requests:
              memory: "32Mi"
              cpu: "25m"
            limits:
              memory: "64Mi"
              cpu: "50m"
        - name: validation-service
          image: verhovnick/validation-service:v4
          ports:
            - containerPort: 8003
          resources:
            requests:
              memory: "32Mi"
              cpu: "25m"
            limits:
              memory: "64Mi"
              cpu: "50m"
